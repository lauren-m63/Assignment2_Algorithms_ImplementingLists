public class SinglyLinkedNode {
    Song data;
    SinglyLinkedNode next;

    public SinglyLinkedNode(Song data) {
        this.data = data;
        this.next = null;
    }
}
// the nodes are just you have a song that is the node and then the next one is nothing a node is just
// one thing so you dont need to connext it or whatever- single so no bcak pointer just the next
